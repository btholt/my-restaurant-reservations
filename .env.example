# Postgres connection string used by drizzle and the app.
# Format: postgresql://<user>@<host>:<port>/<database> (add :<password> after <user> if required)
DATABASE_URL=postgresql://postgres@localhost:5432/my_restaurant_reservations
